# Game content placeholder

Put the complete Noname web package here before building if you want the `.deb`
and `.AppImage` to include the game.

This directory is valid when it contains either `index.html` or `app.html`.
